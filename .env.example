# --- LLM provider keys --------------------------------------------------------
# Used by the agents (LangChain).
ANTHROPIC_API_KEY=your_anthropic_key_here
GOOGLE_API_KEY=your_google_ai_studio_key_here

# --- Service URLs (orchestrator -> agents) ------------------------------------
# Defaults below match `scripts/dev.sh` (non-Docker). docker-compose.yml
# overrides these to internal service DNS names.
AGENT_ROUTER_URL=http://127.0.0.1:8001
AGENT_SCHEMA_FETCHER_URL=http://127.0.0.1:8002
AGENT_PERFORMANCE_ANALYZER_URL=http://127.0.0.1:8003
AGENT_SECURITY_AUDITOR_URL=http://127.0.0.1:8004
AGENT_STYLE_REVIEWER_URL=http://127.0.0.1:8005
AGENT_SYNTHESIZER_URL=http://127.0.0.1:8006

# --- MCP server ---------------------------------------------------------------
MCP_SERVER_URL=http://127.0.0.1:9000/mcp

# --- Frontend (Vite reads VITE_*) --------------------------------------------
# In dev, vite proxies /api to the orchestrator; leaving this unset uses /api.
# VITE_ORCHESTRATOR_URL=http://localhost:8000
